# Copy to `.env` and fill in values.
#
# Secrets (API keys) should live in environment variables and NOT be committed.

# General
AMSCRAPER_LOG_LEVEL=INFO
# Optional default config path used when `--config` is not provided
AMSCRAPER_CONFIG_PATH=configs/example.yaml

# Output directory for exports (CSV/Excel) and raw snapshots
AMSCRAPER_OUTPUT_DIR=out

# Optional: override scheduler cadence (in hours). Must be within 12..24.
# If unset, cadence_hours from the YAML/JSON config is used.
# AMSCRAPER_CADENCE_HOURS=12

# Proxy settings
# Provide either a comma-separated list of proxies OR a file path (one proxy per line).
# Example proxy value: http://user:pass@host:port
AMSCRAPER_PROXIES=
AMSCRAPER_PROXIES_FILE=

# Site credentials
# Site config entries can refer to a specific env var name, e.g. `api_key_env: EXAMPLE_SITE_API_KEY`
EXAMPLE_SITE_API_KEY=
